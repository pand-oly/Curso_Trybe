const numb = 4;
let resultado = 1;
for (let i = numb; i > 0; i --) {resultado *= i;}

// console.log(resultado);

let longestWord('Antônio foi no banheiro e não sabemos o que aconteceu') // retorna 'aconteceu'
